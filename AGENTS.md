# SDK 维护规则

- 修改前检查 Git 状态，保留无关修改。先读 README 和 docs/README.md，再查目标目录规则。
- SDK 只提供插件边界：通用传输、UI 能力、OCR 契约与可选 Worker、专项协议。禁止依赖 soluna-dsl、设备、Appium、原生推理或报告业务。
- UI 能力 1.0、OCR 能力 1.0.0、special/1.0 独立演进。破坏性字段、帧或语义变化必须新协议版本，补兼容方案、规范及夹具。
- 业务真假与 RPC 成功分开；保留 context、身份、资源摘要、错误和清理结果。stdout 只用于协议。
- 默认测试离线，不调用模型、不连接设备。修改执行或生命周期运行 go test -race ./... 与 go vet ./...；同步消费者兼容验收。
- 接口、文档与测试一起维护。SDK 是协议实现的唯一来源，宿主和生成项目直接依赖 SDK，不复制实现。
- 发布固定版本前审阅 go.mod 和测试结果；Go module 不得依赖本机 replace，禁止提交产物、机器路径或 IDE 配置。
